<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="viewModel" type="com.nca.presentation.screen.user.list.UserItemViewModel"/>
    </data>
<RelativeLayout
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:background="@color/colorGreen"
    android:paddingStart="10dp"
    android:paddingEnd="10dp"
    android:layout_marginBottom="5dp">

    <!--<ImageView-->
        <!--android:id="@+id/imageView"-->
        <!--tools:src="@drawable/sova_anmation_1"-->
        <!--android:layout_gravity="start"-->
        <!--android:layout_width="50dp"-->
        <!--android:layout_height="wrap_content" />-->
    <!--tools:src="@drawable/bee"-->

    <TextView
        android:id="@+id/nameTextView"
        android:textSize="14sp"
        android:layout_marginStart="30dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="@{viewModel.name}"/>

    <TextView
        android:id="@+id/ageTextView"
        android:textSize="14sp"
        android:layout_marginStart="230dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="@{viewModel.age}"/>

    <!--<TextView-->
        <!--android:id="@+id/urlTextView"-->
        <!--android:textSize="14sp"-->
        <!--android:layout_marginStart="30dp"-->
        <!--android:layout_marginTop="20dp"-->
        <!--android:layout_width="wrap_content"-->
        <!--android:layout_height="wrap_content"-->
        <!--tools:text="@{viewModel.profileUrl}"/>-->


</RelativeLayout>
</layout>